/**
 * Shared runtime/editor asset catalog for EXPERIENCE
 * Pure procedural visual systems — no external 3D models or GLTF packs
 */

import { defineAssets } from '@iwsdk/core';

export default defineAssets({});
